import { SupplierInterceptor } from './supplier.interceptor';

describe('SupplierInterceptor', () => {
  it('should be defined', () => {
    expect(new SupplierInterceptor()).toBeDefined();
  });
});
